Back-parser

Run:
1) Copy `.env.example` to `.env` and fill `DATABASE_URL`.
2) `npm install`
3) `npm start`

Env:
- `PARSER_BASE_URL` (default `http://host.docker.internal:3000`)
- `LIST_LIMIT` (default 50)
- `DETAIL_DELAY_MS` (default 0)
- `BATCH_SIZE` (default 3)
